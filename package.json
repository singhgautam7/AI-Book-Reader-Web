{
  "name": "ai-book-reader",
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "bun --filter '*' build"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest"
  }
}